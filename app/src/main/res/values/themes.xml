<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Freshly" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- App color system -->
        <item name="colorPrimary">@color/freshly_green</item>
        <item name="colorPrimaryVariant">@color/freshly_green_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/freshly_accent</item>
        <item name="colorSecondaryVariant">@color/freshly_green_light</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>

        <!-- System bars -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- Global text appearances to force black text by default -->
        <item name="textAppearanceBodyLarge">@style/AppTextAppearance.Body</item>
        <item name="textAppearanceBodyMedium">@style/AppTextAppearance.Body</item>
        <item name="textAppearanceBodySmall">@style/AppTextAppearance.Body</item>
        <item name="textAppearanceTitleMedium">@style/AppTextAppearance.Title</item>
        <item name="textAppearanceTitleLarge">@style/AppTextAppearance.Title</item>

        <!-- Default TextInput style with gray hints -->
        <item name="textInputStyle">@style/AppTextInputStyle</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.Freshly.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>
</resources>
