<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Custom styles -->
    <style name="ToolbarTitle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Shape appearance for search bar -->
    <style name="ShapeAppearance.MaterialComponents.MediumComponent" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Global text appearances -->
    <style name="AppTextAppearance.Body" parent="@style/TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="AppTextAppearance.Title" parent="@style/TextAppearance.Material3.TitleMedium">
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Default TextInputLayout/EditText styling -->
    <style name="AppTextInputStyle" parent="@style/Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="android:textColorHint">@color/gray_80</item>
    </style>

    <!-- Clickable Card Style -->
    <style name="ClickableCard">
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>
</resources>
